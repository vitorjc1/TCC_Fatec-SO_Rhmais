<div class="container mt-4">
    {{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
    {{else}}
    {{/each}}

    <div class="card mt-5">
        <div class="card-header colorViolet text-white" style="background-color: #836FFF">
            <h3 class="text-center">Dados do curso</h3>
        </div>
        <div class="card-body" style="background-color: white;">
            <form action="/curso/atualizacao" method="post" class="mt-4">
                <div class="form-group">
                    <label for="formGroupExampleInput">Nome: </label>
                    <input type="text" class="form-control" id="formGroupExampleInput" name="nome"
                       minlength="5" maxlength="50" value="{{curso.dataValues.nome}}">
                </div>
                <input type="hidden" name="id" value="{{curso.dataValues.id}}">
                <div class="row mt-4">
                    <div class="col">
                        <label for="exampleInputCargaHorario">Carga Horária: </label>
                        <input type="number" class="form-control" id="exampleCargaHoraria"
                         min="1" max="1000"   placeholder="Carga Horária..." name="cargaHoraria"
                            value="{{curso.dataValues.cargaHoraria}}" disabled>
                    </div>
                    <div class="col">
                        <label for="exampleInputValor">Valor: </label>
                        <input type="number" class="form-control" id="exampleValor" min="1" placeholder="Valor..." name="valor"
                            value="{{curso.dataValues.valor}}">
                    </div>
                </div>
                <div class="row mt-4">
                    <div class="col">
                        <label for="exampleInputDescricao">Descrição: </label>
                        <textarea type="text" class="form-control" id="exampleInputDescricao"
                            aria-describedby="descricaoHelp" name="descricao" minlength="10" maxlength="250"
                            rows="10">{{curso.dataValues.descricao}}</textarea>
                    </div>
                </div>
                <div class="row mt-4">
                    <div class="col">
                        <button type="submit" class="btn btn-primary">Atualizar</button>
                    </div>
                    <div class="col">

                    </div>
                    <div class="col">

                    </div>
                    <div class="col">
                        <a class="btn btn-warning text-white" data-toggle="modal"
                            data-target="#modalDesativar">Desativar
                            Curso</a>
                    </div>
                </div>
            </form>
        </div>

        <div class="modal fade" id="modalDesativar" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
            aria-hidden="true">
            <div class="modal-dialog" role="document">
                <div class="modal-content">
                    <div class="modal-header" style="background-color: #836FFF;">
                        <h5 class="modal-title text-white" id="exampleModalLabel">Desabilitar Curso</h5>
                        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                    <div class="modal-body">
                        Você tem certeza que deseja desabilitar esse curso? Essa ação não poderá ser desfeita.
                    </div>
                    <div class="modal-footer">
                        <button type="button" class="btn btn-secondary" data-dismiss="modal">Cancelar</button>
                        <a href="/desabilitarCurso/{{curso.dataValues.id}}" type="button"
                            class="btn bg-warning text-white">Desativar</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <a href="/visualizarCurso" class="btn btn-primary mt-4 mb-4"
        style="padding-left: 50px; padding-right: 50px;">Voltar</a>
</div>